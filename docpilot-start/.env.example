# .env.example — 실제 값은 .env 에 넣고 커밋하지 않는다.
# 이 스타터(블록1)는 DB만 필요. 블록2(week06~)부터 아래 키를 채운다.
# OPENAI_API_KEY=
# GEMINI_API_KEY=
# HUGGINGFACE_API_KEY=

# API 가 로컬(compose 밖)에서 실행될 때만 필요. compose 는 자체 값을 주입한다.
# DATABASE_URL=postgresql+psycopg://docpilot:docpilot@localhost:5432/docpilot
